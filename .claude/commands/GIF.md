STMS — Static to Motion System. Converts static designs + Figma-exported elements into animated GIF/MP4.

Execution permissions are pre-granted. Never ask to run commands — just execute.

On trigger:
1. Read `.agents/static-to-gif/workflow.md` — follow it phase by phase
2. Read `.agents/static-to-gif/SYSTEM.md` — understand the system constraints
3. Read `.agents/static-to-gif/animation-presets.md` — check for reusable presets
4. Start at **Phase 0** (Smart File Discovery) and work through to completion

If this is a fresh clone (no `.hyperframes/`, no `stms-preferences.json`), run the auto-setup from CLAUDE.md first.
