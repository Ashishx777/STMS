# Scripts

Helper scripts for the HyperFrames pipeline.

## Active Scripts

None currently — all positioning is handled by the overlay method in the `/GIF` workflow.

## Archive

Legacy Python scripts (OpenCV template matching, pixel diff, position finding) are in `_archive/`. These were used in early development before the overlay method was established. They can still be used as helpers if the overlay method needs supplementary pixel analysis.
